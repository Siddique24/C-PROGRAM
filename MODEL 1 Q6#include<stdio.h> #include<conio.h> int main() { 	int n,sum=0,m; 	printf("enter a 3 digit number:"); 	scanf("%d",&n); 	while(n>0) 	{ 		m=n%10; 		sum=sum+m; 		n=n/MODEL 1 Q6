#include<stdio.h>
#include<conio.h>
int main()
{
	int n,sum=0,m;
	printf("enter a 3 digit number:");
	scanf("%d",&n);
	while(n>0)
	{
		m=n%10;
		sum=sum+m;
		n=n/10;
	}
	printf("sum of 3 digit number=%d",sum);
	getch();
	return 0;
}
